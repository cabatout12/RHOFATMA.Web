@model IEnumerable<RHOFATMA.Web.Models.TblContrat>

@{
    ViewData["Title"] = "Contrats";
}

<div class="d-flex align-items-center justify-content-between mb-3">
    <h1 class="h3 mb-0">Contrats</h1>
    <a asp-action="Create" class="btn btn-primary">Ajouter</a>
</div>

<form asp-action="Index" method="get" class="row g-2 mb-3">
    <div class="col-md-5">
        <input name="search" value="@ViewData["CurrentSearch"]" class="form-control" placeholder="Rechercher un contrat" />
    </div>
    <div class="col-auto"><button type="submit" class="btn btn-outline-primary">Rechercher</button></div>
    <div class="col-auto"><a asp-action="Index" class="btn btn-outline-secondary">Reinitialiser</a></div>
</form>

<table class="table table-hover align-middle">
    <thead><tr><th>Employe</th><th>Type</th><th>Debut</th><th>Fin</th><th>Salaire</th><th>Statut</th><th class="text-end">Actions</th></tr></thead>
    <tbody>
    @foreach (var item in Model)
    {
        <tr>
            <td>@item.IdEmployeNavigation.NomEmploye @item.IdEmployeNavigation.PrenomEmploye</td>
            <td>@item.TypeContrat</td>
            <td>@item.DateDebut.ToString("yyyy-MM-dd")</td>
            <td>@item.DateFin?.ToString("yyyy-MM-dd")</td>
            <td>@item.SalaireBase.ToString("N2")</td>
            <td>@item.Statut</td>
            <td class="text-end">
                <a asp-action="Details" asp-route-id="@item.IdContrat" class="btn btn-sm btn-outline-secondary">Details</a>
                <a asp-action="Edit" asp-route-id="@item.IdContrat" class="btn btn-sm btn-outline-primary">Modifier</a>
                <a asp-action="Delete" asp-route-id="@item.IdContrat" class="btn btn-sm btn-outline-danger">Supprimer</a>
            </td>
        </tr>
    }
    </tbody>
</table>
