@model RHOFATMA.Web.Models.TblEmploye

@{
    ViewData["Title"] = "Modifier un employe";
}

<h1 class="h3 mb-3">Modifier un employe</h1>

<form asp-action="Edit" class="row g-3">
    <input type="hidden" asp-for="IdEmploye" />
    <input type="hidden" asp-for="AjouterPar" />
    <input type="hidden" asp-for="AjouterLe" />
    <partial name="_Form" model="Model" />
    <div class="col-12 d-flex gap-2">
        <button type="submit" class="btn btn-primary">Enregistrer</button>
        <a asp-action="Index" class="btn btn-outline-secondary">Annuler</a>
    </div>
</form>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
